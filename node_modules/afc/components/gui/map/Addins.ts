/**
 * Created by jean.rennes on 11/10/2017.
 */

declare module L {
    export interface GoogleOptions {
        mapOptions: {
            styles: any;
            backgroundColor: string
        }
    }

    export class Google extends Layer {
        constructor(type_?: string, options?: GoogleOptions);
    }

    export interface ButtonOptions {
        text: string;
        iconUrl: string;
        onClick: L.LeafletEventHandlerFn;
        toggle: boolean;
        hideText?: boolean;
        maxWidth?: string;
    }

    export module Control {
        export class Button extends L.Control {
            constructor(options?: Partial<ButtonOptions>);
            public options: L.ControlOptions;
        }
    }
}